syntax = "proto3";

package io.radicalbit.nsdb.rpc;

message Value {
    string stringValue = 1;
    int64 numericValue = 2;
}

message ListAssignment {
    map<string, Value> fields = 1;
}

message ListFields {
    map<string, Value> fields = 1;
}
