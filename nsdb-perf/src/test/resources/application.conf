simulation {
  service {
    host = "http://localhost:9000/",
    read.endpoint = "query/twitterdb"
    write.endpoint = "data"
  },

  scenario {
    translation {
      thread_count = 100
    },
    percent_success = 100
  }
}